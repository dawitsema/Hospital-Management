# Leave empty to use the Vite dev proxy (see vite.config.js).
# For Vercel/production: REQUIRED — set in Vercel → Settings → Environment Variables
# to your Render API origin (no path, no trailing slash), then redeploy:
# VITE_API_URL=https://your-service.onrender.com

VITE_API_URL=
